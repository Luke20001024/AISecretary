# 2026-09-06 公开预览更新

## 本次范围

- 将当前认知主页与记录轨迹迁入公开演示，默认按标签浏览，保留按时间切换。
- 说明页与独立 Demo 共用 `demo/dashboard.html`；历史 HTML 入口继续跳转。
- 说明页截图替换为本次公开预览的浏览器实拍，使用合成示例数据。
- 公开运行时锁定 fixture，禁用本机连接、目录授权与真实写入；21:00 控件说明在线不可用。
- 构建仅接纳白名单普通文件与严格公开配置，拒绝私有 Token、配置覆盖及符号链接。

## 验证

- 8 项 Python 构建/安全测试通过。
- 60 项 Node 回归通过，覆盖公开隔离、数据契约、标签/时间浏览、详情及 UI 状态。
- shared-runtime release contract 通过，生成目录和测试安装包逐字节一致。
- 本地浏览器实测：桌面地图缩放/复位、主题依据、长期理解抽屉、默认标签页及时间切换通过；375px 窄屏无横向页面溢出。
- 说明页 iframe 加载新版演示，实拍截图加载成功。
- 同步两项历史说明页测试的旧结构假设：安装入口位于 `#system`，全屏采用工具栏与 iframe 两行布局；安装链接和全屏尺寸断言继续保留并通过。

## 使用边界

公开站点展示固定的 261 条合成记录，支持交互浏览。快捷键采集、Apple OCR、语音及真实 AI 整理依赖本地安装。本次发布不更新已有下载包，不更改本地安装与个人记录，也不部署个人作品集的其他改动。
